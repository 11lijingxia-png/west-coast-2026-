{
  "name": "west-coast-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file Western US travel handbook",
  "scripts": {
    "build": "node deploy/build-assets.mjs",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
